"""This module defines all global constants to be used throughout the package."""

import datetime as dt

# Define the global timestamp and the format to be used for the file paths.
TIMESTAMP = dt.datetime.now()
TIMESTAMP_FORMAT = "%y_%m_%d_%H_%M_%S"
